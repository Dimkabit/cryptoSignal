# CryptoSignal Proxy Server

Прокси-сервер для получения реальных данных с Binance API без проблем CORS.

## 🚀 Быстрый старт

### Установка зависимостей:
```bash
cd proxy
npm install
```

### Запуск сервера:
```bash
# Производственный режим
npm start

# Режим разработки с автоперезагрузкой
npm run dev
```

Сервер будет доступен на порту 3000: `http://localhost:3000`

## 📋 API Эндпоинты

### Статус сервера
```http
GET /api/status
```

### Получить данные по символу
```http
GET /api/ticker/:symbol
```
Пример: `/api/ticker/BTCUSDT`

### Получить все доступные USDT пары
```http
GET /api/tickers
```

### Получить цены для нескольких символов
```http
POST /api/prices
Content-Type: application/json

{
  "symbols": ["BTCUSDT", "ETHUSDT", "ADAUSDT"]
}
```

### Получить исторические данные
```http
GET /api/history/:symbol?interval=1h&limit=24
```
Параметры:
- `interval`: Интервал (1m, 5m, 15m, 30m, 1h, 4h, 1d)
- `limit`: Количество свечей (1-1000)

## ⚙️ Конфигурация

Сервер имеет следующие настройки:
- **Порт**: 3000 (можно изменить через переменную окружения PORT)
- **CORS**: Включен для всех источников
- **Кэширование**: 30 секунд для снижения нагрузки на API
- **Автоочистка кэша**: Каждые 5 минут

## 🔧 Развертывание

### Локально:
```bash
cd proxy
npm install
npm start
```

### На сервере:
```bash
# Установите зависимости
npm install --production

# Запустите сервер
NODE_ENV=production npm start
```

## 📊 Логирование
Сервер ведет логирование всех запросов и ошибок для удобства отладки.

## 🔒 Безопасность
- Сервер не требует аутентификации для чтения данных
- Рекомендуется использовать reverse proxy (nginx) в продакшене
- Ограничьте доступ к серверу по IP при необходимости